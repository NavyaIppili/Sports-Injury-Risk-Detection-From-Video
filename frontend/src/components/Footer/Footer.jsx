import styles from './Footer.module.css';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Built for an internship milestone with a modular architecture for future AI, database, and video workflows.</p>
    </footer>
  );
}
